FROM registry.access.redhat.com/ubi9/python-312:latest

WORKDIR /app

COPY pyproject.toml .
RUN pip install --no-cache-dir uv && uv sync --no-dev

COPY src/ src/

ENV PYTHONPATH=/app/src
EXPOSE 8000

CMD ["uvicorn", "student_rag_playground.main:app", "--host", "0.0.0.0", "--port", "8000"]
